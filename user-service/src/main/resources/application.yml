server:
  port: 10002

spring:
  application:
    name: user-service

# 日志级别
logging:
  level:
    com.qizhi: INFO

# Knife4j 文档
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
knife4j:
  enable: true
